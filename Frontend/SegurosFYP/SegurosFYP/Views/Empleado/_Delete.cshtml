@model SegurosFYP.Models.EmpleadoViewModel

        @{
        ViewData["Title"] = "_Delete";
        ViewData["ModelName"] = "EmpleadoViewModel".Replace("ViewModel","");
        }
        <h3>Estas seguro que deseas eliminar este registro?</h3>
            <div class="modal-footer">
                <hr />
                <form asp-action="Delete" asp-controller="Empleado">
                    <input type="hidden" name="Emple_Id" id="id" />
                    <hr />
                    <div class="form-group">
                        <input type="submit" value="Eliminar" class="btn btn-danger" />
                        <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
                    </div>
                </form>
            </div>
            <script>
                function ObtenerID(id) {
                    console.log("si entra" + id);
                    $("#id").val(id);

                }
            </script>
